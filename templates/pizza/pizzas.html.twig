{% extends 'base.html.twig' %}

{% block title %}Carte des Pizzas{% endblock %}

{% block body %}
<div class="container mt-5 mb-5">
   
    <div class="text-center mb-5">
        <div class="d-flex flex-wrap justify-content-center gap-3">
            <a href="#pizzas" class="btn btn-outline-danger rounded-pill px-5 py-3 fs-4 fw-bold">🍕 Pizzas</a>
            <a href="#desserts" class="btn btn-outline-warning rounded-pill px-5 py-3 fs-4 fw-bold">🍰 Desserts</a>
            <a href="#boissons" class="btn btn-outline-info rounded-pill px-5 py-3 fs-4 fw-bold">🥤 Boissons</a>
        </div>
    </div>

    <div class="row align-items-center mb-5">
        <div class="col-md-7">
            <h1 class="fw-bold text-danger display-5">🍕 Commande de pizzas</h1>
            <p class="text-muted fs-5">
                Envie d'une pizza maison, généreuse et pleine de saveurs ?<br>
                Commandez en ligne parmi nos recettes préparées avec des produits frais et de qualité. 
            </p>
        </div>
        <div class="col-md-5 text-end">
            <img src="{{ asset('images/pizza/pizza1.jpg') }}" alt="Pizza" class="img-fluid rounded-4" style="max-height: 200px;">
        </div>
    </div>

    <!-- SECTION PIZZAS -->
    <h2 id="pizzas" class="fw-bold text-danger mb-4 border-bottom pb-2">Nos Pizzas</h2>
    <div class="row g-4 justify-content-center mb-5">
        {% for pizza in pizzas %}
            <div class="col-md-6 col-lg-4">
                <div class="card shadow rounded-4 h-100" style="background-color: #f5f0e6;">
                    {% if pizza.image %}
                        <img src="{{ asset('uploads/pizzas/' ~ pizza.image) }}" 
                             class="card-img-top rounded-top-4" 
                             alt="{{ pizza.nom }}" 
                             style="height: 220px; object-fit: cover;">
                    {% else %}
                        <div class="card-img-top rounded-top-4 d-flex align-items-center justify-content-center bg-light" 
                             style="height: 220px; border-bottom: 1px solid #dee2e6;">
                            <div class="text-center text-muted">
                                <i class="fas fa-camera fa-3x mb-2"></i>
                                <p class="mb-0 fw-bold">Photo à venir</p>
                            </div>
                        </div>
                    {% endif %}

                    <div class="card-body d-flex flex-column">
                        <h3 class="card-title fw-bold text-danger">{{ pizza.nom }}</h3>
                        <p class="text-secondary small mb-2">
                            {{ pizza.description|default('Aucune description disponible.')|slice(0, 70) ~ '...' }}
                        </p>
                        <a href="{{ path('pizza_show', {'id': pizza.id}) }}" class="text-decoration-none small text-danger mb-3">
                            En savoir plus
                        </a>
                        <h4 class="fw-bold text-dark mb-2">{{ pizza.prix }} €</h4>

                        <button class="btn btn-danger btn-sm rounded-4 mt-auto btn-ajouter-panier"
                                data-id="{{ pizza.id }}"
                                data-nom="{{ pizza.nom }}"
                                data-prix="{{ pizza.prix }}">
                            🛒 Ajouter au panier
                        </button>
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-center text-muted fs-5">Aucune pizza disponible pour le moment 😢</p>
        {% endfor %}
    </div>

    <!-- SECTION DESSERTS -->
    <h2 id="desserts" class="fw-bold text-warning mb-4 border-bottom pb-2">🍰 Nos Desserts</h2>
    <div class="row g-4 justify-content-center mb-5">
        <!-- Tiramisu -->
        <div class="col-md-6 col-lg-4">
            <div class="card shadow rounded-4 h-100" style="background-color: #fff9e6;">
                <img src="{{ asset('images/desserts/tiramisu.jpg') }}" 
                     class="card-img-top rounded-top-4" 
                     alt="Tiramisu" 
                     style="height: 220px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h3 class="card-title fw-bold text-warning">Tiramisu</h3>
                    <p class="text-secondary small mb-2">
                        Le classique italien : biscuits imbibés de café, mascarpone crémeux et cacao.
                    </p>
                    <h4 class="fw-bold text-dark mb-2">5.50 €</h4>
                    <button class="btn btn-warning btn-sm rounded-4 mt-auto btn-ajouter-panier"
                            data-id="dessert-1"
                            data-nom="Tiramisu"
                            data-prix="5.50">
                        🛒 Ajouter au panier
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- SECTION BOISSONS -->
    <h2 id="boissons" class="fw-bold text-info mb-4 border-bottom pb-2">🥤 Nos Boissons</h2>
    <div class="row g-4 justify-content-center">
        <!-- Coca -->
        <div class="col-md-6 col-lg-3">
            <div class="card shadow rounded-4 h-100" style="background-color: #e6f7ff;">
                <img src="{{ asset('images/boissons/coca.png') }}" 
                     class="card-img-top rounded-top-4" 
                     alt="Coca-Cola" 
                     style="height: 220px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h3 class="card-title fw-bold text-info">Coca-Cola</h3>
                    <p class="text-secondary small mb-2">
                        Canette 33cl - La boisson gazeuse iconique.
                    </p>
                    <h4 class="fw-bold text-dark mb-2">2.50 €</h4>
                    <button class="btn btn-info btn-sm rounded-4 mt-auto btn-ajouter-panier"
                            data-id="boisson-1"
                            data-nom="Coca-Cola"
                            data-prix="2.50">
                        🛒 Ajouter au panier
                    </button>
                </div>
            </div>
        </div>

        <!-- Ice Tea -->
        <div class="col-md-6 col-lg-3">
            <div class="card shadow rounded-4 h-100" style="background-color: #e6f7ff;">
                <img src="{{ asset('images/boissons/icetea.jpg') }}" 
                     class="card-img-top rounded-top-4" 
                     alt="Ice Tea" 
                     style="height: 220px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h3 class="card-title fw-bold text-info">Ice Tea</h3>
                    <p class="text-secondary small mb-2">
                        Canette 33cl - Thé glacé au citron rafraîchissant.
                    </p>
                    <h4 class="fw-bold text-dark mb-2">2.50 €</h4>
                    <button class="btn btn-info btn-sm rounded-4 mt-auto btn-ajouter-panier"
                            data-id="boisson-2"
                            data-nom="Ice Tea"
                            data-prix="2.50">
                        🛒 Ajouter au panier
                    </button>
                </div>
            </div>
        </div>

        <!-- Sprite -->
        <div class="col-md-6 col-lg-3">
            <div class="card shadow rounded-4 h-100" style="background-color: #e6f7ff;">
                <img src="{{ asset('images/boissons/sprite.jpg') }}" 
                     class="card-img-top rounded-top-4" 
                     alt="Sprite" 
                     style="height: 220px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h3 class="card-title fw-bold text-info">Sprite</h3>
                    <p class="text-secondary small mb-2">
                        Canette 33cl - Boisson citron-lime sans caféine.
                    </p>
                    <h4 class="fw-bold text-dark mb-2">2.50 €</h4>
                    <button class="btn btn-info btn-sm rounded-4 mt-auto btn-ajouter-panier"
                            data-id="boisson-3"
                            data-nom="Sprite"
                            data-prix="2.50">
                        🛒 Ajouter au panier
                    </button>
                </div>
            </div>
        </div>

        <!-- Orangina -->
        <div class="col-md-6 col-lg-3">
            <div class="card shadow rounded-4 h-100" style="background-color: #e6f7ff;">
                <img src="{{ asset('images/boissons/orangina.jpg') }}" 
                     class="card-img-top rounded-top-4" 
                     alt="Orangina" 
                     style="height: 220px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h3 class="card-title fw-bold text-info">Orangina</h3>
                    <p class="text-secondary small mb-2">
                        Bouteille 33cl - Boisson gazeuse à l'orange et pulpe.
                    </p>
                    <h4 class="fw-bold text-dark mb-2">3.00 €</h4>
                    <button class="btn btn-info btn-sm rounded-4 mt-auto btn-ajouter-panier"
                            data-id="boisson-4"
                            data-nom="Orangina"
                            data-prix="3.00">
                        🛒 Ajouter au panier
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
.cart-count {
    position: absolute;
    top: -3px;
    right: -5px;
    background: #28a745;
    color: white;
    font-size: 0.75rem;
    font-weight: bold;
    border-radius: 50%;
    width: 18px;
    height: 18px;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: 0 0 4px rgba(0,0,0,0.2);
    transition: transform 0.2s ease;
}
.cart-count.bump {
    transform: scale(1.3);
}

/* Style pour la navigation fluide */
html {
    scroll-behavior: smooth;
}
</style>
{% endblock %}


