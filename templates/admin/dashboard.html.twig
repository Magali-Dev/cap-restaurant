{% extends 'base.html.twig' %}

{% block title %}Tableau de bord Admin - Le Cap Ristobar{% endblock %}

{% block body %}
<div class="admin-container">
    <div class="admin-header">
        <h1>Tableau de bord Administrateur</h1>
        <p>Gestion du restaurant Le Cap Ristobar</p>
    </div>

    <div class="admin-stats">
        <div class="stat-card">
            <div class="stat-icon">👥</div>
            <div class="stat-content">
                <h3>Utilisateurs total</h3>
                <p class="stat-number">{{ users_count }}</p>
                <a href="{{ path('admin_users') }}" class="stat-link">Gérer les utilisateurs</a>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">👑</div>
            <div class="stat-content">
                <h3>Administrateurs</h3>
                <p class="stat-number">{{ admins_count }}</p>
                <a href="{{ path('admin_users') }}" class="stat-link">Voir les admins</a>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">📅</div>
            <div class="stat-content">
                <h3>Réservations total</h3>
                <p class="stat-number">{{ reservations_count }}</p>
                <a href="{{ path('admin_reservations') }}" class="stat-link">Voir toutes les réservations</a>
            </div>
        </div>

        <div class="stat-card">
            <div class="stat-icon">🕐</div>
            <div class="stat-content">
                <h3>Réservations aujourd'hui</h3>
                <p class="stat-number">{{ reservations_today }}</p>
                <a href="{{ path('admin_reservations') }}" class="stat-link">Voir les réservations du jour</a>
            </div>
        </div>
    </div>

    <!-- Section gestion des réservations -->
    <div class="dashboard-card">
        <div class="card-header">
            <h3>📅 Gestion des réservations</h3>
        </div>
        <div class="card-content">
            <p>Gérez l'ensemble des réservations et contrôlez la disponibilité en ligne.</p>
        </div>
        <div class="card-actions">
            <a href="{{ path('admin_reservations') }}" class="btn btn-primary">
                Voir toutes les réservations
            </a>
            <a href="{{ path('admin_reservation_limits') }}" class="btn btn-outline">
                ⚙️ Limiter les réservations
            </a>
        </div>
    </div>

    <!-- Section Actions rapides -->
    <div class="admin-actions">
        <h2>Actions rapides</h2>
        <div class="action-buttons">
            <a href="{{ path('admin_users') }}" class="btn btn-primary">
                👥 Gérer les utilisateurs
            </a>
            <a href="{{ path('admin_reservations') }}" class="btn btn-primary">
                📅 Gérer les réservations
            </a>
            <a href="{{ path('admin_gallery') }}" class="btn btn-primary">
                🖼️ Gérer la gallery
            </a>
            <a href="{{ path('admin_menus') }}" class="btn btn-primary">
                📋 Gérer les menus et les suggestions
            </a>
            <a href="{{ path('admin_events') }}" class="btn btn-primary">
                🎪 Gérer les événements
            </a>
            <a href="{{ path('admin_pizzas_index') }}" class="btn btn-primary">
            🍕 Gérer les cartes pizza
            </a>
            <a href="{{ path('admin_supplements_index') }}" class="btn btn-primary">
                🍄 Gérer les suppléments
            </a>
        </div>
    </div>
</div>
{% endblock %}
