<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Restaurant Le Cap - Accueil{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🍕</text></svg>">
        
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        {# Navigation simple #}
        <header style="background: #2c3e50; color: white; padding: 1rem;">
            <nav style="display: flex; justify-content: space-between; align-items: center; max-width: 1200px; margin: 0 auto;">
                <h1 style="margin: 0;">🍕 Restaurant Le Cap</h1>
                <div>
                    <a href="{{ path('app_home') }}" style="color: white; margin: 0 1rem; text-decoration: none;">Accueil</a>
                    <a href= "#" style="color: white; margin: 0 1rem; text-decoration: none;">Menu</a>
                    <a href="#" style="color: white; margin: 0 1rem; text-decoration: none;">Réservation</a>
                    <a href="#" style="color: white; margin: 0 1rem; text-decoration: none;">Contact</a>
                </div>
            </nav>
        </header>

        {# Contenu principal #}
        <main style="min-height: 80vh; padding: 2rem;">
            {% block body %}
                <div style="text-align: center; padding: 4rem 0;">
                    <h2>Bienvenue au Restaurant Le Cap</h2>
                    <p>Une expérience culinaire exceptionnelle</p>
                    <a href="#" style="background: #e74c3c; color: white; padding: 1rem 2rem; text-decoration: none; border-radius: 5px; display: inline-block; margin-top: 1rem;">
                        Réserver une table
                    </a>
                </div>
            {% endblock %}
        </main>

        {# Pied de page #}
        <footer style="background: #34495e; color: white; text-align: center; padding: 2rem; margin-top: 2rem;">
            <p>&copy; {{ "now"|date("Y") }} Restaurant Le Cap - Tous droits réservés</p>
            <p>📞 01 23 45 67 89 | 📍 123 Avenue du Restaurant, 75000 Paris</p>
        </footer>
    </body>
</html>